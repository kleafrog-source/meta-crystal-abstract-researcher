install:
	pnpm install

run-dev:
	portless player next dev

run-dev-clean:
	rm -rf .next && portless player next dev

build:
	pnpm build

test:
	pnpm vitest run

lint:
	pnpm lint

.PHONY: install run-dev run-dev-clean build test lint
